# Taskify — Task Manager with Local AI Assistant

Веб-приложение для управления задачами с локальным LLM-ассистентом на базе Llama через Ollama.

## Реализованный функционал

- **CRUD задач** — создание, просмотр, редактирование, удаление
- **Фильтрация и поиск** — по статусу, приоритету, полнотекстовый поиск
- **Виды** — Все задачи / Сегодня / Ближайшие 7 дней / Срочные
- **AI: умная категоризация** — предлагает тег/категорию по названию и описанию
- **AI: декомпозиция** — разбивает сложную задачу на подзадачи
- **AI: приоритет** — рекомендует приоритет на основе описания и срока
- **AI: сводка нагрузки** — естественно-языковой анализ текущих задач
- **AI: план дня** — анализирует все задачи и предлагает порядок работы на сегодня

## Стек

| Слой | Технология |
|---|---|
| Frontend + Backend | Next.js 16 (App Router) |
| База данных | SQLite через Prisma 5 |
| LLM | Local Llama через Ollama |
| Стили | Tailwind CSS |
| Язык | TypeScript |

## Настройка и запуск

### 1. Клонирование репозитория

```bash
git clone <repo-url>
cd task-manager
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Локальная Llama

Для AI-функций установите [Ollama](https://ollama.com) и скачайте Llama-модель:

```bash
ollama pull llama3.2
```

Приложение само обратится к `http://127.0.0.1:11434` и выберет установленную Llama-модель. Если Ollama не запущен, AI-сценарии не ломаются: включается локальная эвристика без внешних ключей.

### 4. Инициализация базы данных

```bash
npx prisma db push
```

### 5. Запуск

```bash
npm run dev
```

Приложение доступно на [http://localhost:3000](http://localhost:3000).

## Структура проекта

```
app/
  api/
    tasks/          — CRUD задач (GET, POST, PUT, DELETE)
    llm/
      categorize/   — AI категоризация
      decompose/    — AI декомпозиция
      plan/          — AI план дня
      priority/     — AI приоритет
      summary/      — AI сводка нагрузки
  page.tsx          — главная страница
  layout.tsx        — корневой layout

components/
  Sidebar.tsx       — боковая навигация
  TaskCard.tsx      — карточка задачи
  TaskForm.tsx      — форма создания/редактирования
  TaskFilters.tsx   — панель фильтров
  LlmPanel.tsx      — панель AI-функций
  SummaryPanel.tsx  — блок сводки нагрузки
  Modal.tsx         — модальное окно

lib/
  prisma.ts         — синглтон Prisma Client
  llama.ts          — клиент локальной Llama через Ollama
  taskHeuristics.ts — fallback-логика AI без LLM
  types.ts          — общие TypeScript-типы

prisma/
  schema.prisma     — схема данных
```

## Архитектурные решения

- **Разделение ответственности**: бизнес-логика в API routes, UI — в компонентах, доступ к БД — через Prisma-слой в `lib/prisma.ts`.
- **RESTful API**: стандартные HTTP-методы (GET, POST, PUT, DELETE), корректные статус-коды (200, 201, 400, 404, 500), структурированные ошибки.
- **Фильтрация на сервере**: все параметры фильтрации передаются в запросы к БД, а не обрабатываются на клиенте.
- **Singleton Prisma Client**: предотвращает исчерпание пула соединений при hot-reload в dev-режиме.
- **Local-first AI**: Llama работает через локальный Ollama без API-ключей в репозитории; при недоступности модели приложение возвращает эвристический fallback.
- **Prompt Engineering**: системные промпты с явными ролями и требованием JSON-вывода там, где это улучшает качество ответа.

## Известные ограничения

- Файловая БД SQLite не подходит для многопользовательского продакшена — при необходимости схема Prisma легко переключается на PostgreSQL.
- Для полноценного LLM-ответа на компьютере должен быть запущен Ollama с Llama-моделью.
- Аутентификация не реализована (не входила в ТЗ).

## Что можно добавить

- Теги и проекты для группировки задач
- Drag & drop для ручной расстановки приоритетов
- Push-уведомления о просроченных задачах
- Экспорт в CSV / iCal
- Авторизация (NextAuth.js)
